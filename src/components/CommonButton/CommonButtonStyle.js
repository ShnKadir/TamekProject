import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button: {
        height: 40,
        width: "100%",
        alignItems: "center",
        justifyContent: "center",
        borderRadius: 8,
        padding: 8
    },

    text: {
        fontSize: 15,
        lineHeight: 20,
        textAlign: "center",
        fontWeight: "600"
    },
})