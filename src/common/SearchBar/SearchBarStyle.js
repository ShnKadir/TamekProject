import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    container: {
        borderRadius: 8,
        height: 30,
        alignItems:"center",
    },
    text: {
        fontSize: 14,
        color: "#bfbfbf"
    }
})